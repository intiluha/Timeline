from base import *


def postgame(screen: pygame.Surface, results: Dict[str, int], timeline: List[int], cards: List[Card]) -> bool:
    # TODO save results to table
    # TODO show results of last game and timeline
    # TODO? show results of all games
    # TODO choose one of three options: restart, new game and exit
    print(results)
    return True
